- add repository url in pod
- can only use an existing player as a facade
- use variable $o to set facade
- add logs to say that an agent change facade
- Note is a REF?
- Don't discard a player if he doesn't have agents at the end of the turn
- trunk the output of an agent to 10 characters
- Produce DTD
- Cell id in hexadecimal (heh heh heh)
- Single CSS file for everyone
- in configuration.xps, check if open-ended
- For empty cells, show 'empty'
- Ownership array
- Free admission games (no need to register beforehand)
